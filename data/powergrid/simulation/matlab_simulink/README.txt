MATLAB and Simulink outputs for cross-checks against the Python swing tool layer (optional).

Add run notes (script name, version, date) next to .mat or exported time series. Not required for the default pipeline.
