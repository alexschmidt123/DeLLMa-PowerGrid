#!/usr/bin/env bash
# Historical note: this repo previously ran `evaluate_dellma.py` here for paper reproduction.
# That script was removed; use `python main.py ...` per README to generate results.

echo "See README.md for main.py usage (agent_name farmer | trader | grid)."
